import '../schema/schema.dart';

// A helper class for sql building stuffs
class SqlBuilder {

  // Takes a schema and produces a table create statement
  String tableStatement(Schema schema) {
    return '';
  }
}
